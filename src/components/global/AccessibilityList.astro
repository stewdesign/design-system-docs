---
// Plain list of accessibility requirements.
export interface Props {
  items: string[];
}

const { items } = Astro.props;
---
<section id="accessibility" aria-label="Accessibility">
  <h2>Accessibility</h2>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</section>
